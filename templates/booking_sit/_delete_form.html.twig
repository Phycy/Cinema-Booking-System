<form method="post" action="{{ path('app_booking_sit_delete', {'id': booking_sit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking_sit.id) }}">
    <button class="btn">Delete</button>
</form>
