<form method="post" action="{{ path('app_showtime_delete', {'id': showtime.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ showtime.id) }}">
    <button class="btn">Delete</button>
</form>
